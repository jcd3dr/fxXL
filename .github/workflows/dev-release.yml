name: fxXL Dev Release (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unsupported:
    runs-on: ubuntu-latest
    steps:
      - run: echo "fxXL does not publish a dev update channel. Use stable GitHub Releases."
