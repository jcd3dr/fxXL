name: fxXL CDN Backfill (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unsupported:
    runs-on: ubuntu-latest
    steps:
      - run: echo "fxXL publishes directly to GitHub Releases and has no CDN backfill."
