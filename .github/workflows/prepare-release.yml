name: fxXL Prepare Release (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unsupported:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bump FXXL_VERSION in a reviewed commit; the main release workflow publishes it."
